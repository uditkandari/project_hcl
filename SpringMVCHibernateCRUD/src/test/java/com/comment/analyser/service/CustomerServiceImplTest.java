package com.comment.analyser.service;

public class CustomerServiceImplTest 
{
	/*
	 * @Test void testGetCustomer() { CustomerDAO customerDAO =
	 * EasyMock.createMock(CustomerDAO.class);
	 * EasyMock.expect(customerDAO.getCustomer(1)).andReturn(null);
	 * EasyMock.replay(null); CustomerServiceImpl customerServiceImpl = new
	 * CustomerServiceImpl(); customerServiceImpl.setCustomerDAO(customerDAO);
	 * Customer expected = customerServiceImpl.getCustomer(1);
	 * assertNotNull(expected); assertEquals(expected, null);
	 * 
	 * }
	 */
	
}
